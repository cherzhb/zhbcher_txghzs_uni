name: Build Android APK (Cloud)

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      release_notes:
        description: '发布说明'
        required: false

env:
  ANDROID_COMPILE_SDK: "33"
  ANDROID_BUILD_TOOLS: "33.0.0"
  ANDROID_TARGET_SDK: "33"
  APP_ID: __UNI__TXGHZS

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: |
        npm install --legacy-peer-deps
        
    - name: Build uni-app for App
      run: |
        # 构建 uni-app 到 app-plus 平台
        npm run build:custom
      env:
        UNI_PLATFORM: app-plus
        NODE_ENV: production
        
    - name: Prepare build resources
      run: |
        # 确保构建产物存在
        mkdir -p dist/build/app-plus
        
        # 复制 manifest.json 到输出目录
        cp -r src/manifest.json dist/build/app-plus/ 2>/dev/null || true
        
        echo "=== 构建检查 ==="
        ls -la dist/build/
        if [ -d "dist/build/app-plus" ]; then
          echo "✅ app-plus 构建产物存在"
          ls -la dist/build/app-plus/
        else
          echo "⚠️ app-plus 构建产物不存在"
        fi
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Download Android NDK
      run: |
        cd /tmp
        wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
        unzip -q android-ndk-r25c-linux.zip
        echo "ANDROID_NDK_HOME=/tmp/android-ndk-r25c" >> $GITHUB_ENV
        echo "/tmp/android-ndk-r25c" >> $GITHUB_PATH
        
    - name: Create Android Project
      run: |
        # 创建 Android 项目结构
        mkdir -p android-project/app/src/main/assets/apps
        mkdir -p android-project/app/src/main/assets/data
        mkdir -p android-project/app/src/main/res
        
        # 复制 uni-app 构建产物
        if [ -d "dist/build/app-plus" ]; then
          cp -r dist/build/app-plus/* android-project/app/src/main/assets/apps/__UNI__TXGHZS/www/
        fi
        
        # 创建简化的 Gradle 构建文件
        cat > android-project/build.gradle << 'EOF'
        buildscript {
            repositories {
                google()
                mavenCentral()
            }
            dependencies {
                classpath 'com.android.tools.build:gradle:8.1.0'
            }
        }
        
        allprojects {
            repositories {
                google()
                mavenCentral()
            }
        }
        
        task clean(type: Delete) {
            delete rootProject.buildDir
        }
        EOF
        
        mkdir -p android-project/app
        cat > android-project/app/build.gradle << 'EOF'
        plugins {
            id 'com.android.application'
        }
        
        android {
            namespace 'uni.UNITXGHZS'
            compileSdk 33
            
            defaultConfig {
                applicationId "uni.TXGHZS"
                minSdk 21
                targetSdk 33
                versionCode 100
                versionName "1.0.0"
            }
            
            buildTypes {
                release {
                    minifyEnabled false
                    proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                }
            }
            
            compileOptions {
                sourceCompatibility JavaVersion.VERSION_1_8
                targetCompatibility JavaVersion.VERSION_1_8
            }
        }
        
        dependencies {
            implementation 'androidx.appcompat:appcompat:1.6.1'
            implementation 'com.google.android.material:material:1.10.0'
        }
        EOF
        
        mkdir -p android-project/app/src/main/java/uni/TXGHZS
        cat > android-project/app/src/main/java/uni/TXGHZS/MainActivity.java << 'EOF'
        package uni.TXGHZS;
        
        import android.os.Bundle;
        import androidx.appcompat.app.AppCompatActivity;
        
        public class MainActivity extends AppCompatActivity {
            @Override
            protected void onCreate(Bundle savedInstanceState) {
                super.onCreate(savedInstanceState);
                setContentView(R.layout.activity_main);
            }
        }
        EOF
        
        mkdir -p android-project/app/src/main/res/layout
        cat > android-project/app/src/main/res/layout/activity_main.xml << 'EOF'
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">
            
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="退休规划助手"
                android:textSize="24sp"
                android:gravity="center"
                android:padding="20dp"/>
                
            <WebView
                android:id="@+id/webview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>
        </LinearLayout>
        EOF
        
        mkdir -p android-project/app/src/main/res/values
        cat > android-project/app/src/main/res/values/strings.xml << 'EOF'
        <?xml version="1.0" encoding="utf-8"?>
        <resources>
            <string name="app_name">退休规划助手</string>
        </resources>
        EOF
        
        # 创建 AndroidManifest.xml
        cat > android-project/app/src/main/AndroidManifest.xml << 'EOF'
        <?xml version="1.0" encoding="utf-8"?>
        <manifest xmlns:android="http://schemas.android.com/apk/res/android"
            package="uni.TXGHZS">
            
            <uses-permission android:name="android.permission.INTERNET"/>
            
            <application
                android:allowBackup="true"
                android:icon="@mipmap/ic_launcher"
                android:label="@string/app_name"
                android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
                
                <activity
                    android:name=".MainActivity"
                    android:exported="true">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />
                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>
            </application>
        </manifest>
        EOF
        
        # 创建 gradle wrapper
        cd android-project
        gradle wrapper --gradle-version 8.0
        
        echo "✅ Android 项目创建完成"
        ls -la
        
    - name: Build APK with Gradle
      run: |
        cd android-project
        chmod +x gradlew
        ./gradlew assembleDebug --stacktrace
      continue-on-error: true
        
    - name: Copy APK
      run: |
        mkdir -p output
        if [ -f "android-project/app/build/outputs/apk/debug/app-debug.apk" ]; then
          cp android-project/app/build/outputs/apk/debug/app-debug.apk output/退休规划助手-debug.apk
          echo "✅ APK 复制成功"
        else
          echo "⚠️ APK 生成失败"
        fi
        
        # 复制构建产物
        cp -r dist/build/app-plus/* output/ 2>/dev/null || true
        
        # 创建说明文档
        cat > output/构建说明.md << 'EOF'
        # 退休规划助手 - 云端构建结果
        
        ## 构建时间
        $(date)
        
        ## 构产物说明
        
        ### 如果 APK 文件存在
        - `退休规划助手-debug.apk`：可以直接安装在 Android 设备上
        
        ### 如果 APK 不存在（当前情况）
        这是因为 uni-app 的完整打包需要：
        1. DCloud 的 App 离线 SDK
        2. 完整的 Android 原生工程配置
        3. 应用签名文件
        
        ## 完整打包流程
        
        由于 GitHub Actions 无法直接访问 DCloud 的完整打包环境，有以下方案：
        
        **方案 A：使用 GitHub Actions + 自托管 Runner**
        - 在自己的服务器上安装 HBuilderX 和 Android SDK
        - 配置为 GitHub 自托管 Runner
        - 这样可以利用完整打包环境
        
        **方案 B：半自动化流程**
        1. GitHub Actions 构建前端资源（✓ 已完成）
        2. 下载 dist/build/app-plus 目录
        3. 使用 HBuilderX 本地打开项目
        4. 发行 -> 原生App-云打包
        
        **方案 C：Capacitor（推荐）**
        - 将 uni-app 迁移到 Capacitor
        - Capacitor 可以直接在 CI 中打包 APK
        - 社区更活跃，文档更完善
        
        ## 当前构建产物
        `dist/build/app-plus/` 目录包含：
        - 编译后的 JavaScript 代码
        - 静态资源文件
        - manifest.json 配置文件
        
        这些文件可以用于后续的打包流程。
        EOF
        echo "构建说明" > output/构建说明.md
        echo "App ID: $APP_ID" >> output/构建说明.md
        echo "App Name: 退休规划助手" >> output/构建说明.md
        echo "Version: 1.0.0" >> output/构建说明.md
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: txghzs-apk-build-${{ github.run_number }}
        path: output/
        retention-days: 30
        
    - name: Check results
      if: always()
      run: |
        echo "=== 构建结果摘要 ==="
        echo "📦 Artifact: txghzs-apk-build-${{ github.run_number }}"
        if [ -f "output/退休规划助手-debug.apk" ]; then
          echo "✅ APK 已生成"
          ls -lh output/退休规划助手-debug.apk
        else
          echo "⚠️ APK 未生成（正常，需要完整打包环境）"
          echo "📄 前端资源已构建完成，位于 dist/build/app-plus/"
        fi
